// const filterByNumber = (filter, planets, set) => {
//   console.log('chamei o filterByNumber', filter);
//   set(planets.filter((planet) => planet[filter.column] > filter.value));
// };

const filterByNumber = (filter) => {
  console.log(filter);
};

export default filterByNumber;
